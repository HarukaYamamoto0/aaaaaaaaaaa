export enum ConnectionMode {
    Adapter,
    Wired
}

export enum Button {
    LEFT = 0,
    RIGHT = 1,
    MIDDLE = 2,
    FORWARD = 3,
    BACKWARD = 4,
    DPI = 5,
    SCROLL_UP = 6,
    SCROLL_DOWN = 7
}